# 🏗️ Architecture Overview

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                       │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │         src/app/page.js (Home Page)                │    │
│  │                                                     │    │
│  │  ┌──────────────────────────────────────────┐     │    │
│  │  │  src/components/chat/Chatbox.jsx         │     │    │
│  │  │                                           │     │    │
│  │  │  ┌─────────────┐  ┌──────────────────┐  │     │    │
│  │  │  │PromptMessage│  │ResponseMessage   │  │     │    │
│  │  │  └─────────────┘  └──────────────────┘  │     │    │
│  │  └──────────────────────────────────────────┘     │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                           │
│                                                              │
│  ┌────────────────────────────────────────────────────┐    │
│  │     src/lib/api/chatService.js                     │    │
│  │                                                     │    │
│  │  • sendMessage(prompt)                             │    │
│  │  • Error handling                                  │    │
│  │  • Request/Response transformation                 │    │
│  └────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    CONFIGURATION LAYER                       │
│                                                              │
│  ┌──────────────────────┐  ┌──────────────────────────┐   │
│  │  src/config/env.js   │  │ src/lib/config/          │   │
│  │                      │  │ constants.js             │   │
│  │  • apiBaseUrl        │  │                          │   │
│  │  • appName           │  │ • API_ENDPOINTS          │   │
│  │  • appVersion        │  │ • MESSAGES               │   │
│  │                      │  │ • BOT_CONFIG             │   │
│  └──────────────────────┘  └──────────────────────────┘   │
│              ↑                                              │
│              │                                              │
│  ┌──────────────────────┐                                  │
│  │   .env.local         │  (NOT in Git)                    │
│  │                      │                                  │
│  │  NEXT_PUBLIC_API_    │                                  │
│  │  BASE_URL=...        │                                  │
│  └──────────────────────┘                                  │
└─────────────────────────────────────────────────────────────┘
                            │
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      BACKEND API                             │
│                                                              │
│              http://localhost:5455/ai/chat                   │
│                                                              │
│              (Java Spring Boot Server)                       │
└─────────────────────────────────────────────────────────────┘
```

## Component Hierarchy

```
App
└── RootLayout (src/app/layout.js)
    ├── HomePage (src/app/page.js)
    │   └── Chatbox (src/components/chat/Chatbox.jsx)
    │       ├── PromptMessage (src/components/chat/PromptMessage.jsx)
    │       └── ResponseMessage (src/components/chat/ResponseMessage.jsx)
    │
    └── AboutPage (src/app/about/page.jsx)
```

## Request Flow

```
1. User types message in Chatbox
   ↓
2. Chatbox.handleSendMessage() called
   ↓
3. chatService.sendMessage(prompt) invoked
   ↓
4. Service reads API URL from env.apiBaseUrl
   ↓
5. Axios POST request to backend
   ↓
6. Backend processes and responds
   ↓
7. Service returns data to component
   ↓
8. Component updates state
   ↓
9. UI re-renders with response
```

## Configuration Flow

```
.env.local (secrets)
    ↓
process.env (Next.js)
    ↓
src/config/env.js (wrapper)
    ↓
Components & Services
```

## File Dependencies

```
Chatbox.jsx
├── imports PromptMessage.jsx
├── imports ResponseMessage.jsx
├── imports chatService.js
│   └── imports env.js
│       └── reads .env.local
└── imports constants.js
```

## Security Layers

```
┌─────────────────────────────────────┐
│  Layer 1: .gitignore                │
│  Prevents committing secrets        │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  Layer 2: .env.local                │
│  Stores secrets locally             │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  Layer 3: src/config/env.js         │
│  Provides safe access               │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  Layer 4: Service Layer             │
│  Centralizes API calls              │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│  Layer 5: Components                │
│  Clean, no hardcoded values         │
└─────────────────────────────────────┘
```

## Scalability Pattern

### Adding New Features

```
New Feature (e.g., "Trading")
│
├── Create: src/components/trading/
│   ├── TradingPanel.jsx
│   └── TradeForm.jsx
│
├── Create: src/lib/api/tradingService.js
│   └── API methods
│
├── Update: src/lib/config/constants.js
│   └── Add TRADING_ENDPOINTS
│
└── Create: src/app/trading/page.jsx
    └── New page route
```

## Best Practices Applied

```
✅ Separation of Concerns
   • UI (Components)
   • Logic (Services)
   • Config (Constants & Env)

✅ Single Responsibility
   • Each file has one purpose
   • Components focus on UI
   • Services handle API

✅ DRY (Don't Repeat Yourself)
   • Constants in one place
   • Reusable components
   • Shared services

✅ Security First
   • No hardcoded secrets
   • Environment variables
   • Proper .gitignore

✅ Maintainability
   • Clear folder structure
   • Consistent naming
   • Good documentation
```

## Technology Stack

```
┌─────────────────────────────────────┐
│         Frontend Stack              │
├─────────────────────────────────────┤
│ • Next.js 16 (React Framework)      │
│ • React 19 (UI Library)             │
│ • Tailwind CSS 4 (Styling)          │
│ • Axios (HTTP Client)               │
│ • tsParticles (Animations)          │
└─────────────────────────────────────┘
              ↕
┌─────────────────────────────────────┐
│         Backend Stack               │
├─────────────────────────────────────┤
│ • Java Spring Boot                  │
│ • REST API                          │
│ • Port 5455                         │
└─────────────────────────────────────┘
```

## Development Workflow

```
1. Edit Code
   ↓
2. Hot Reload (npm run dev)
   ↓
3. Test in Browser
   ↓
4. Check Console for Errors
   ↓
5. Commit Changes
   ↓
6. Push to Repository
   ↓
7. Deploy to Production
```

---

This architecture ensures:
- 🔒 **Security** - Proper secret management
- 📦 **Modularity** - Easy to modify and extend
- 🧪 **Testability** - Services can be mocked
- 📚 **Maintainability** - Clear structure
- 🚀 **Scalability** - Ready for growth
